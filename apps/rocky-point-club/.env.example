# Stage two — Supabase (member directory, auth, gated content)
# Not used in stage one (public marketing site). Copy to .env.local when wiring auth.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=
